import { useState } from 'react'; import { exampleWall } from '../../content/reed-smith/evidence';
export function EvidenceWall(){const [active,setActive]=useState('ex-observation');return <section id="evidence-wall" aria-labelledby="wall"><h3 id="wall">Evidence wall <span className="rs-tag rs-tag--demo">Demonstration data</span></h3><p>These neutral records demonstrate structure only. They are not Reed Smith findings.</p><div className="rs-wall">{exampleWall.map((x,i)=><button className={active===x.id?'is-active':''} onClick={()=>setActive(x.id)} key={x.id}><small>{i+1} / {x.evidenceClass}</small><span>{x.claim}</span><em>{x.limitations[0]}</em></button>)}</div></section>}
