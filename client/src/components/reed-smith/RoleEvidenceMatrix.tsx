import { useState } from 'react'; import { roleProofs } from '../../content/reed-smith/caseStudy';
export function RoleEvidenceMatrix(){const [active,setActive]=useState(0);return <section className="rs-section" aria-labelledby="matrix"><p className="rs-index">03 / Fit with candor</p><h2 id="matrix">Role-to-proof matrix</h2><p className="rs-copy">Select a signal to inspect the proof and the boundary beside it.</p><div className="rs-matrix"><div className="rs-tablist" role="tablist" aria-label="Role signals">{roleProofs.map((r,i)=><button role="tab" aria-selected={active===i} aria-controls="proof-panel" id={`proof-${i}`} tabIndex={active===i?0:-1} onClick={()=>setActive(i)} key={r.signal}>{r.signal}</button>)}</div><article className="rs-proof" role="tabpanel" id="proof-panel" aria-labelledby={`proof-${active}`}><h3>{roleProofs[active].signal}</h3>{[['Existing evidence','evidence'],['Project','project'],['Proof type','proofType'],['Confidence','confidence'],['Transferability','transferability'],['Known gap','gap']].map(([label,key])=><div key={key}><dt>{label}</dt><dd>{String(roleProofs[active][key as keyof typeof roleProofs[number]])}</dd></div>)}</article></div></section>}
