import { useState } from 'react';
const axes=['Desirability','Feasibility','Client value','Professional judgment','Risk','Evidence confidence','Reversibility','Learning value'];
export function DecisionMatrix(){const [scores,setScores]=useState(axes.map((_,i)=>[3,4,3,5,2,2,5,4][i]));return <section aria-labelledby="decision-matrix"><h3 id="decision-matrix">Decision matrix <span className="rs-tag rs-tag--demo">Demonstration values</span></h3><p>Illustrative controls—not Reed Smith scoring. Risk is a cost dimension; all others are benefit dimensions.</p><div className="rs-scores">{axes.map((x,i)=><label key={x}><span>{x}<output>{scores[i]} / 5</output></span><input type="range" min="1" max="5" value={scores[i]} onChange={e=>setScores(s=>s.map((v,j)=>j===i?Number(e.target.value):v))}/></label>)}</div></section>}
